from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Building a classifier to predict if college NBA player will be drafted based on statistics',
    author='Rushab Shah',
    license='BSD-3',
)
